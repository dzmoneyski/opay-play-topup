# نظام إدارة التحقق من الهوية - دليل المشرف

## نظرة عامة
تم إنشاء نظام إداري متكامل للتحقق من وثائق المستخدمين وتفعيل حساباتهم. يتكون النظام من:

### المكونات الرئيسية:

1. **نظام الأدوار (User Roles)**
   - جدول `user_roles` لإدارة أدوار المستخدمين
   - دورين أساسيين: `admin` و `user`
   - وظائف أمان للتحقق من الأدوار

2. **لوحة الإدارة (Admin Panel)**
   - صفحة `/admin` خاصة بالمشرفين فقط
   - عرض إحصائيات الطلبات
   - إدارة طلبات التحقق المعلقة

3. **نظام الموافقة/الرفض**
   - وظائف قاعدة بيانات آمنة للموافقة والرفض
   - تحديث حالة التحقق تلقائياً
   - إمكانية إضافة سبب الرفض

## كيفية إضافة مشرف جديد:

### الطريقة 1: عبر SQL
```sql
-- إدراج دور admin لمستخدم معين
INSERT INTO public.user_roles (user_id, role) 
VALUES ('USER_ID_HERE', 'admin');
```

### الطريقة 2: عبر Supabase Dashboard
1. افتح Supabase Dashboard
2. اذهب إلى Table Editor
3. اختر جدول `user_roles`
4. أضف صف جديد:
   - `user_id`: معرف المستخدم من جدول auth.users
   - `role`: admin

## الوصول إلى لوحة الإدارة:

1. **للمشرفين**: ستظهر أيقونة لوحة الإدارة (🛡️) في الهيدر
2. **الرابط المباشر**: `/admin`
3. **الحماية**: يتم منع الوصول للمستخدمين العاديين تلقائياً

## ميزات لوحة الإدارة:

### الإحصائيات:
- إجمالي الطلبات
- الطلبات المعلقة
- الطلبات الموافق عليها
- الطلبات المرفوضة

### إدارة الطلبات:
- **الموافقة**: تفعيل فوري للحساب
- **الرفض**: إضافة سبب الرفض وإشعار المستخدم
- **عرض التفاصيل**: اسم المستخدم، رقم الهوية، رقم الهاتف، تاريخ التقديم

## تدفق عمل التحقق:

1. **المستخدم يقدم طلب التحقق**: يرسل رقم الهوية الوطنية
2. **ظهور الطلب في لوحة الإدارة**: يُدرج في قسم "طلبات تحتاج مراجعة"
3. **مراجعة المشرف**: يراجع البيانات ويقرر الموافقة أو الرفض
4. **التحديث التلقائي**: يتم تحديث حالة المستخدم والحساب تلقائياً

## الأمان والصلاحيات:

- **Row Level Security (RLS)**: محمي بالكامل
- **التحقق من الأدوار**: وظائف آمنة للتحقق من صلاحيات المشرف
- **الوصول المحدود**: لوحة الإدارة متاحة للمشرفين فقط

## الصيانة والمراقبة:

### مراقبة الأداء:
- تتبع عدد الطلبات المعلقة
- مراقبة أوقات الاستجابة
- إحصائيات الموافقة/الرفض

### النسخ الاحتياطي:
- جميع البيانات محفوظة في Supabase
- سجل كامل للمراجعات والقرارات

## المشاكل الشائعة وحلولها:

### لا أستطيع الوصول إلى لوحة الإدارة:
1. تأكد من أن لديك دور `admin` في جدول `user_roles`
2. تحقق من تسجيل الدخول
3. امسح cache المتصفح

### الطلبات لا تظهر:
1. تحقق من RLS policies
2. تأكد من أن المستخدمين يقدمون طلبات التحقق
3. تحديث الصفحة

### خطأ في الموافقة/الرفض:
1. تحقق من صلاحيات قاعدة البيانات
2. تأكد من أن المستخدم لديه دور admin
3. راجع console logs للأخطاء

## الدعم الفني:

لأي مشاكل تقنية، راجع:
1. Supabase Dashboard → Functions → Logs
2. Browser Developer Tools → Console
3. Network tab للأخطاء في API calls